
<div class="row">
	<h2 align="center">Dashboard</h2>
<ul class="tabs" data-tabs="tabs" >
	<li class="active">
		<a href="#Overdue">Overdue</a>
	</li>
	<li>
		<a href="#Today">Today</a>
	</li>
	<li>
		<a href="#Pending">Pending</a>
	</li>
	<li>
		<a href="#Completed">Completed</a>
	</li>
</ul>
<div id="my-tab-content" class="tab-content">
	<div class="active tab-pane" id="Overdue">
		<table>
			<tr>
				<th>Name</th>
				<th>Due Date</th>
				<th>Context</th>
				<th>Status</th>
				<th></th>
			</tr>
			<% @tasks.each do |task| %>
			<% if task.duedate < DateTime.now.to_date  && task.status == 'OVERDUE'%>
			<%= render :partial => "tasks/tasks", :locals => {:task => task} %>
			<% end %>
			<% end %>
		</table>
	</div>
	<div class="tab-pane" id="Today">
		<table>
			<tr>
				<th>Name</th>
				<th>Due Date</th>
				<th>Context</th>
				<th>Status</th>
				
				<th></th>
			</tr>
			<% @tasks.each do |task| %>
			<% if task.duedate == DateTime.now.to_date && task.status == 'PENDING'%>
			<%= render :partial => "tasks/tasks", :locals => {:task => task} %>
			<% end %>
			<% end %>
		</table>
	</div>
	<div class="tab-pane" id="Pending">
		<table>
			<tr>
				<th>Name</th>
				<th>Due Date</th>
				<th>Context</th>
				
				<th>Status</th>
				
				<th></th>
			</tr>
			<% @tasks.each do |task| %>
			<% if task.duedate > DateTime.now.to_date  && task.status == 'PENDING' %>
			<%= render :partial => "tasks/tasks", :locals => {:task => task} %>
			<% end %>
			<% end %>
		</table>
	</div>
	<div class="tab-pane" id="Completed">
		<table>
			<tr>
				<th>Name</th>
				<th>Due Date</th>
				<th>Context</th>
				
				<th>Status</th>
				
				<th></th>
			</tr>
			<% @tasks.each do |task| %>
			<% if task.status == 'COMPLETED' %>
			<%= render :partial => "tasks/tasks", :locals => {:task => task} %>
			<% end %>
			<% end %>
		</table>
	</div>
</div>
</div>